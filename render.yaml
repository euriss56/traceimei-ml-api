services:
  - type: web
    name: traceimei-ml-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: TRACEIMEI_API_KEY
        generateValue: true
    autoDeploy: true
